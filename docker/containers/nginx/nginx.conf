http {
    upstream backends {
        least_conn; # Manda pra quem tem menos conexões ativas (aplicavel quando tem mais de um backend)
        server trigger:9000 max_fails=2 fail_timeout=30s;
    }

    server {
        listen 80;

        server_name localhost;

        root /var/www/html/public;
        index index.php;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;

            fastcgi_pass backends;

            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        }
    }
}

events {}
